{
  "name": "nuzzle-desktop",
  "version": "0.3.0",
  "private": true,
  "description": "A native floating desktop companion for coding agents",
  "scripts": {
    "frontend:build": "python3 scripts/build_frontend.py",
    "test:python": "python3 -m unittest discover -s scripts -p 'test_*.py'",
    "test:ui": "python3 scripts/verify_app.py",
    "pets:audit": "python3 scripts/audit_all_pets.py",
    "dev": "npm run frontend:build && tauri dev",
    "build": "npm run frontend:build && tauri build",
    "build:universal": "npm run frontend:build && tauri build --target universal-apple-darwin --bundles dmg",
    "release:macos": "sh scripts/build_macos_release.sh",
    "check": "npm run frontend:build && npm run pets:audit && npm run test:python && cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
